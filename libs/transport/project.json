{
  "name": "transport",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/transport/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:transport"],
  "targets": {
    "lint": { "executor": "@nx/eslint:lint" },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/transport/tsconfig.lib.json --noEmit"
      }
    }
  }
}
