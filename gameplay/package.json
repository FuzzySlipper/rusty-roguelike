{
  "name": "rusty-roguelike-gameplay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json && node scripts/materialize.mjs",
    "check": "pnpm build && git diff --exit-code -- ../data/gameplay/"
  }
}
